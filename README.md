# lab_video
create new branch
ติดตั้ง venv
เปิดใช้งาน .venv
สร้าง .gitignore
upgrade pip
install pandas & dbt-duckdb
pip freeze
create dbt project
เอา datasets มาใส่ใน northwind
create staging folder
จัดการไฟล์ sql,yml ในโฟลเดอร์ staging ให้ตรงกับ datasets
จัดการไฟล์ products
debug & run (เข้าไปรันใน northwind)
create query_duckdb.py & run query
install streamlit
ทำหน้า website (app.py)
run app.py
create folder datawarehouse ใน model
create dim_.sql & schema.yml
สร้างแดชบอร์ดและแสดงแดชบอร์ด
git add , git commit , git push & git merge
